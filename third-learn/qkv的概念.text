对于 x个元素 n维的词向量

表示为矩阵 X

对于每个元素 X[i] 还会有一个 A[i]权重 用于生成注意力矩阵。 注意力矩阵代表了个元素间的相对关心

对于 元素2

需要计算其注意力矩阵 共有 x列----》相对每关系

计算为 a1*x1+a2*x2.....加全求和 得到注意力矩阵

这里 有三个计算的矩阵

一个是参数 a1的计算 权重矩阵A的计算中
每个元素都作为了 查询者 被查询者
在最终计算时 又作为了值输出计算 所以衍生了3个矩阵 避免语义冲突

这样看 可以理解为 乘以上不同值 后 代表了 x向量在不同语义中的词嵌入